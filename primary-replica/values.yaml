name: primary-replica
container:
  port: 5432
  name:
    primary: primary
    replica: replica
credentials:
  primary: password
  root: password
  user: password
serviceType: ClusterIP
image: registry.access.redhat.com/rhscl/postgresql-10-rhel7
pvc:
  name: primary-pvc
resources:
  cpu: 200m
  memory: 512Mi
